# ROSA 绒花计划 Wiki — Quartz 5 开发日志

> 项目：将 Obsidian 知识库转换为 Quartz 5 静态网站  
> 基础框架：Quartz v5.0.0 (https://quartz.jzhao.xyz)  
> 开发阶段：本地预览与调试  

---

## 2026-07-25 开发与调试

### 1. 配置文件中文化

**文件**：`quartz.config.yaml`

| 配置项 | 修改前 | 修改后 | 说明 |
|--------|--------|--------|------|
| `locale` | `en-US` | `zh-CN` | 界面语言切换为简体中文 |
| `pageTitle` | `ROSA` | `ROSA 绒花计划Wiki` | 站点标题全称 |
| `theme.fontOrigin` | `googleFonts` | `local` | 字体本地化，避免外部 CDN 依赖 |
| `theme.typography.header` | `Schibsted Grotesk` | 中文字体栈 | 适配中文显示 |
| `theme.typography.body` | `Source Sans Pro` | 中文字体栈 | 适配中文显示 |
| `theme.typography.code` | `IBM Plex Mono` | 中文字体栈 | 适配中文显示 |
| `explorer.options.title` | — | `导航` | 侧边栏导航标题自定义 |
| `explorer.options.folderClickBehavior` | — | `link` | 文件夹点击行为 |
| `explorer.options.folderDefaultState` | — | `collapsed` | 文件夹默认折叠 |
| `explorer.options.useSavedState` | — | `true` | 保存折叠状态 |

---

### 2. 页眉横幅与 Logo

**文件**：`quartz/styles/custom.scss`

- 添加 `body::before` 伪元素作为顶部横幅，显示站点标题
- 桌面端：大尺寸居中 Logo（`page-title a::before`）
- 移动端（≤800px）：28×28px 小图标适配
- 暗色模式：自动切换为 `logo-white.svg`

**文件**：`quartz/static/`

| 文件 | 用途 |
|------|------|
| `logo.svg` | 浅色模式 Logo |
| `logo-white.svg` | 暗色模式 Logo |
| `icon.png` | 站点 Favicon |

---

### 3. 页脚链接扩展

**文件**：`quartz.config.yaml` → `footer.options.links`

新增链接：
- `常见问答` → `/02-帮助/常见问答`

保留原有链接：
- `CC BY-NC-SA 4.0` → 知识共享许可
- `免责声明` → `/02-帮助/免责声明`
- `联系我们` → `/02-帮助/联系我们`
- `关于本站` → `/02-帮助/关于`

---

### 4. 表格边框增强

**文件**：`quartz/styles/custom.scss`

```scss
table, th, td {
  border: 1px solid var(--gray) !important;
}
```

提升表格边框在浅色模式下的可见度。

---

### 5. 回到顶部按钮

**需求**：界面右侧悬浮按钮，滚动后显示，点击平滑返回顶部。

#### 5.1 实现过程

| 步骤 | 方案 | 结果 |
|------|------|------|
| 1 | React `onClick` 绑定事件 | ❌ SPA 导航后事件丢失 |
| 2 | HTML `onclick` 属性 | ❌ TypeScript 类型报错 |
| 3 | `dangerouslySetInnerHTML` 注入 `<button>` | ✅ 最终采用 |

#### 5.2 最终代码

**`quartz/components/Body.tsx`**：

```tsx
<div
  dangerouslySetInnerHTML={{
    __html: `<button id="back-to-top" onclick="window.scrollTo(0, 0)" aria-label="回到顶部">↑</button>`,
  }}
/>
<script
  dangerouslySetInnerHTML={{
    __html: `
      (function() {
        if (window.__backToTopInitialized) return;
        window.__backToTopInitialized = true;
        document.addEventListener('DOMContentLoaded', function() {
          var btn = document.getElementById('back-to-top');
          if (!btn) return;
          window.addEventListener('scroll', function() {
            if (window.scrollY > 300) {
              btn.classList.add('visible');
            } else {
              btn.classList.remove('visible');
            }
          });
        });
      })();
    `,
  }}
/>
```

**`quartz/styles/custom.scss`**：

```scss
#back-to-top {
  position: fixed;
  bottom: 2rem;
  right: 2rem;
  z-index: 999;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s ease;
  background-color: var(--secondary);
  color: white;
  border: none;
  border-radius: 50%;
  width: 3rem;
  height: 3rem;
  font-size: 1.5rem;
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

#back-to-top.visible {
  opacity: 1;
  pointer-events: auto;
}
```

#### 5.3 关键问题记录

**问题**：点击按钮后页面不滚动。

**根因**：
1. Quartz SPA 导航通过 micromorph 替换 `document.body`，React 事件绑定在导航后断开
2. 纯 HTML `onclick` 在 TSX 中触发 TypeScript 类型错误（`ButtonHTMLAttributes` 不含 `onclick`）

**解决**：使用 `dangerouslySetInnerHTML` 注入原生 `<button>` 元素，绕过 React 事件系统，确保 SPA 导航后依然有效。

---

### 6. 内容文件更新

| 文件 | 操作 | 说明 |
|------|------|------|
| `content/index.md` | 修改 | 添加 Frontmatter `title: "首页"` |
| `content/02 帮助/常见问答.md` | 新建 | 完整 FAQ 页面（含 `publish: true`） |

---

### 7. 被否决的方案

| 方案 | 否决原因 |
|------|----------|
| 自定义组件 `TestComponent.tsx` | Quartz v5 不支持 YAML 直接挂载自定义组件 |
| 修改 `quartz.layout.ts` | 该文件不存在，布局由 YAML 驱动 |
| 修改 `quartz/components/Header.tsx` | 按钮注入位置由 `Body.tsx` 更合理 |
| CSS `scroll-behavior: smooth` + JS `behavior: "smooth"` 双重滚动 | 浏览器中产生冲突，视觉上无效 |

---

### 8. Git 提交记录

```
187b003  自定义页眉标题、暗色页眉、页脚链接、表格边框样式
138aaf9  备份：配置文件（.gitignore, quartz, 许可证等）
137cf6a  backup: 应用配色补丁A（深靛蓝扁平化风格），网站运行稳定
10d567b  first commit
```

---

### 9. 当前未提交改动（`git status`）

**已修改**：
- `quartz.config.yaml` — 配置中文化、页脚链接、字体、Explorer 选项
- `quartz/components/Body.tsx` — 回到顶部按钮及滚动监听脚本
- `quartz/styles/custom.scss` — 移动端适配、回到顶部按钮样式

**新增文件**：
- `quartz/static/logo.svg`
- `quartz/static/logo-white.svg`

**未追踪**：
- `content/02 帮助/常见问答.md`（已创建但未 `git add`）

---

### 10. 已知问题

| 问题 | 状态 | 备注 |
|------|------|------|
| TypeScript 报错 `micromorph` 模块声明无法解析 | 框架级预存 | 不影响构建 |
| TypeScript 报错 `remark-parse/lib` 模块未找到 | 框架级预存 | 不影响构建 |
| `常见问答.md` 未被 Git 追踪 | 待处理 | 警告：日期将不准确 |
| `public/` 目录偶尔无法被 `rm -rf` 清理 | 偶发 | 可能有残留进程占用 |

---

*最后更新：2026-07-25*
